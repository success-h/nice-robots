# Copy to .env.deploy and fill in (do not commit .env.deploy).
# Same Entra app / subscription as wisebuddy is fine for auth — but ACR is SEPARATE from Aisha.

export AZURE_TENANT_ID=
export AZURE_CLIENT_ID=
export AZURE_CLIENT_SECRET=
export AZURE_SUBSCRIPTION_ID=

# Optional — only if `az acr credential show` fails (ACR admin user disabled):
# export ACR_REGISTRY_PASSWORD=

# Defaults (NiceRobots frontend — NOT aishacr):
# export AZURE_RG=aisharg
# export AZURE_LOCATION=northeurope
# export CONTAINER_APP_NAME=nicerobotsfe
# export CONTAINER_APP_ENV=aishaenv
# export ACR_NAME=nicerobotsfeacr
# export IMAGE_REPO=nicerobotsfe
# export TARGET_PORT=3000

# Build-time public env (point at the Azure API Container App after first API deploy):
# export NEXT_PUBLIC_GOOGLE_CLIENT_ID=
# export NEXT_PUBLIC_BACKEND_URL=https://nicerobotsapi.<env>.<region>.azurecontainerapps.io/api
# export NEXT_PUBLIC_WS_URL=wss://nicerobotsapi.<env>.<region>.azurecontainerapps.io/socket
# export NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
